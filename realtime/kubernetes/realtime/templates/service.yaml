apiVersion: v1
kind: Service
metadata:
  name: {{ include "realtime.fullname" . }}
  labels:
    app: {{ include "realtime.name" . }}
    chart: {{ include "realtime.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  sessionAffinity: ClientIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "realtime.name" . }}
    release: {{ .Release.Name }}
